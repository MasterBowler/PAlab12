package compulsory;

import javax.swing.*;
import java.awt.*;

/**
 * The design panel of the application. Allows the visualisation of the components.
 */
public class DesignPanel extends JPanel {
    public DesignPanel() {
        setPreferredSize(new Dimension(800, 600));
    }
}
